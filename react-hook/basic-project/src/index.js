import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import AppMentorsImmer from './basic-immer/AppMentorsImmer';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <AppMentorsImmer />
  </React.StrictMode>,
);
